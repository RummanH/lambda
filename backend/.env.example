# Port the NestJS server listens on
PORT=3000

# TODO(AWS): Once API Gateway + Lambda are set up, this server may be
# deployed as a Lambda handler instead of a standalone Express server,
# or run behind API Gateway as a proxy. No AWS config is needed yet.
